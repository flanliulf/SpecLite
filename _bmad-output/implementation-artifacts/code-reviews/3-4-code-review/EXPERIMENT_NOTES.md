# EXPERIMENT_NOTES

## 实时笔记

- 2026-05-28: 前置核对显示 Epic 3 的 `3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation` 当前为 `ready-for-dev`。工作区中该 Story 文件已有既有未提交改动，执行时必须保留并在其基础上继续。
- 2026-05-28: `python3` resolver 因 Xcode Python 3.9 缺少 `tomllib` 失败；改用 `python3.12` 成功解析 workflow。workflow 无 prepend/append，persistent fact 为 `file:{project-root}/**/project-context.md`。
- 2026-05-28: 当前进度文件中原计划包含 CR/fixer/finalizer/提交，已按本轮用户要求收敛为 dev-story-only；后续 CR 不在本轮执行。
- 2026-05-28: 前置代码核对发现 `package.json`、`src/bin/speclite.ts`、`src/commands/validate.ts`、manifest/schema、adapter registry、diagnostics、path-normalizer、safe-write、fixture contract 已存在；`runtime-path.ts` 与 `legacy-namespace.ts` 尚不存在，需要在 Story 3.4 范围内新增。
- 2026-05-28: 保守实现决策：runtime validation 以 installed `files-index.json` 中 `_speclite/config.toml`、`_speclite/config.user.toml`、runtime/config/custom/script entries 和 manifest `paths.specliteRoot` 为检查入口；legacy validation 只检查 known `_bmad` residue、files-index legacy references 和与当前 canonical skill id 重叠的 stale skill entry，不做全项目任意扫描。
- 2026-05-28: 验证结果：`npm run build` 通过；focused tests `runtime-path-validation`、`legacy-namespace-validation`、`menu-target-validation`、`artifact-path-validation`、`validate-command` 共 25 项通过；`git diff --check` 通过。
- 2026-05-28: HALT blocker：完整 `npm test` 失败 2 项：`test/ide-target-writer.test.ts` canonical package hash expected `sha256:a73efc...` actual `sha256:6c6cc...`；`test/runtime-structure.test.ts` expected `sha256:477272...` actual `sha256:cabe9...`。失败指向既有 asset/fixture hash drift，不属于 Story 3.4 修改范围；按用户要求不修改无关 fixtures，Story 不推进 `review`。
- 2026-05-28: fresh sub-agent 复核结论：`npm test -- --run test/ide-target-writer.test.ts test/runtime-structure.test.ts` 仍失败 2 项；focused Story 3.4 tests 仍通过 25 项。`runtime-structure` 的 `skill-index-speclite-dev-story` hash 可视为 canonical package hash baseline drift 候选，但 `ide-target-writer` 的失败由当前 hash/include 逻辑与实际复制面不一致触发：`hashPackageDirectory(sourceRoot, { include: isInstallableCanonicalPackageFile })` 跳过未直接匹配 include 的 `references` 目录，得到 `sha256:a73efc...`；安装目录实际包含 `references/details.md`，目录 hash 为 `sha256:6c6cc...`。这不是单纯更新两个 fixture 文件即可安全解决的 deterministic baseline drift；修复需要修改 `src/manifest/hash.ts` 的目录遍历/include 语义或重新定义该测试期望，超出本轮允许范围。决策：继续 HALT，不改源码、不改 hash fixture、不推进 `review`。
- 2026-05-28: 收尾验证：`npm run build` 通过；完整 `npm test` 仍失败同 2 项、其余 22 个测试文件 139 项通过；`git diff --check` 通过。Story 3.4 focused tests 已通过，但完整回归未通过，因此 Story 和 sprint status 必须保持 `in-progress`。
- 2026-05-28: 用户授权本轮只处理该 HALT 的最小技术修复。决策更新：失败根因不是单纯 fixture drift，而是 shared hash helper include traversal bug；`src/manifest/hash.ts` 不应把 file-level include predicate 当作目录剪枝 predicate。
- 2026-05-28: 修复方式：`listFiles()` 对目录始终递归，仍只把 included file 加入返回列表，且只对 included symlink 抛出 canonical package hash 错误。这样 canonical hash included file set 与 `copyCanonicalPackage()` 的 installable package surface 对齐，同时保留 Story 3.3 对非 canonical adapter artifact symlink 的过滤语义。
- 2026-05-28: 验证结果：`npm test -- --run test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 2/2 文件 12/12 测试；Story 3.4 focused tests 通过 5/5 文件 25/25 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 141/141 测试；`git diff --check` 通过。
- 2026-05-28: HALT 解除，未更新 hash fixture；Story 文件和 `sprint-status.yaml` 已推进到 `review`。
- 2026-05-28: CR round 1 启动。Story 文件定位为 `_bmad-output/implementation-artifacts/stories/3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation.md`；CR 目录为 `_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/`；未发现既有 summary，轮次为 1。
- 2026-05-28: 审查范围限定为 Story 3.4 File List 中的源码与测试文件；进度文件、Story 文件和 sprint status 只作为上下文，不纳入代码 finding 范围。
- 2026-05-28: 当前环境未提供 Agent 子代理工具，按 `bmenhance-cr-01-reviewer` 降级规则改为串行执行 Blind Hunter、Edge Case Hunter、Acceptance Auditor。
- 2026-05-28: CR 验证命令：`npm run build` 通过；focused/targeted `npm test -- --run test/runtime-path-validation.test.ts test/legacy-namespace-validation.test.ts test/menu-target-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 7/7 文件 37/37 测试；完整 `npm test` 通过 24/24 文件 141/141 测试；`git diff --check` 通过；仓库没有 `lint` script。
- 2026-05-28: CR round 1 结论：发现 3 个 patch findings，其中 2 个中优先级 AC 覆盖缺口、1 个低优先级诊断精度问题。`src/manifest/hash.ts` HALT 最小修复未发现扩大范围问题。结果文件：`_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/3-4-code-review-summary-20260528-round-1.md`。
- 2026-05-28: CR evaluation round 1 启动。按 `bmenhance-cr-02-evaluator` 配置确认 review source 为 `3-4-code-review-summary-20260528-round-1.md`，本轮 evaluation 编号为 1；未读取或评估其他 Story 的 CR 结果。
- 2026-05-28: 评估证据只读核对：`validateProject` 的 artifact aggregation 未传入 metadata/actual artifact path；`validateArtifactPathContract` 只有 `input.metadata !== undefined` 时才校验 required metadata；`validateLegacyNamespace` 未调用已存在的 `installedSkillReferencesLegacyConfig()`；`runtime-path` 遇到任一 symlink segment 即报 `runtime-path.symlink-escape`，未解析 target 是否实际逃出项目边界。
- 2026-05-28: 保守评估决策：3 个 patch findings 均成立，且均对应 Story 3.4 AC 覆盖或诊断语义，不降级为 CR TODO。第 3 项原 review 严重性为低，但 AC1 明确要求“解析后逃出”才报 escape，评估提升为 P1 修复项。
- 2026-05-28: CR evaluation round 1 结论：不通过；需修复项 3 个，TODO/记录项 0 个，误报 0 个；可进入 `bmenhance-cr-03-fixer`。
- 2026-05-28: CR fixer round 1 启动。最新 evaluation 文件为 `3-4-code-review-evaluation-20260528-round-1.md`；仅修复其中 3 个“需要修复”项，不处理非评估确认问题，不修改 Story 文档。
- 2026-05-28: 修复 #1 决策：在 `validateProject` artifact aggregation 中只读发现 `artifactContract.defaultOutputPath` 下的 artifact files，解析 Markdown frontmatter 或 sidecar JSON metadata，并把 `actualArtifactPath` 与 metadata 传给 `validateArtifactPathContract()`；无 artifact file 时保留原 contract path validation。
- 2026-05-28: 修复 #2 决策：基于 `skillIndex.entries[].installedTargets` 与 adapter registry 精确定位当前 installed canonical `SKILL.md`，调用既有 `installedSkillReferencesLegacyConfig()`；不做全项目任意扫描，不把历史目录纳入判断。
- 2026-05-28: 修复 #3 决策：`runtime-path` 遇到 symlink segment 时使用 `realpath` 与 project root realpath 比较，只有 symlink target 逃出 target project boundary 才报告 `runtime-path.symlink-escape`；项目内 symlink 不再误报。
- 2026-05-28: CR fixer 验证结果：`npm test -- --run test/runtime-path-validation.test.ts test/legacy-namespace-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts` 通过 4/4 文件 23/23 测试；`npm run build` 通过；`npm test` 通过 24/24 文件 144/144 测试；`git diff --check` 通过。
- 2026-05-28: CR round 2 复审启动。Story 文件定位为 `_bmad-output/implementation-artifacts/stories/3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation.md`；CR 目录为 `_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/`；已有 1 个 review summary，本轮为 Round 2。
- 2026-05-28: Round 2 复审上下文：Round 1 的 3 个 patch 均在 evaluation 修复执行记录中标记已修复，修复点分别为 production artifact metadata 读取、installed canonical `SKILL.md` legacy config reference 检查、runtime symlink realpath boundary 分类。
- 2026-05-28: 当前环境未提供 Agent 子代理工具，按 `bmenhance-cr-01-reviewer` 降级规则改为串行执行 Blind Hunter、Edge Case Hunter、Acceptance Auditor。
- 2026-05-28: CR round 2 验证命令：focused `npm test -- --run test/runtime-path-validation.test.ts test/legacy-namespace-validation.test.ts test/menu-target-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 7/7 文件 40/40 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 144/144 测试；`git diff --check` 通过；仓库没有 `lint` script。
- 2026-05-28: CR round 2 结论：Round 1 Finding #2 和 #3 已闭环；Finding #1 对 Markdown frontmatter / file sidecar JSON 已闭环，但 directory artifact `metadata.json` 仍未被 production discovery / artifact rule 覆盖。定向 rule-level 复现中，`actualArtifactPath` 指向包含非法 `metadata.json` 的 directory artifact 返回 `[]`。结果文件：`_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/3-4-code-review-summary-20260528-round-2.md`。
- 2026-05-28: CR evaluation round 2 启动。按 `bmenhance-cr-02-evaluator` 配置确认 review source 为 `3-4-code-review-summary-20260528-round-2.md`，已有 evaluation round 1，本轮 evaluation 编号为 2；未读取或评估其他 Story 的 CR 结果。
- 2026-05-28: 评估证据只读核对：Story AC5 要求 workflow artifact metadata 缺失或值域非法必须报告 reserved issue，Artifact Path Contract Notes 要求 Directory artifacts 在 artifact directory 内写出 `metadata.json`；当前 `validate-project.ts` 的 directory discovery 只递归列出普通 files，明确跳过 `metadata.json` / `*.metadata.json`，`readWorkflowArtifactMetadata()` 只读取 Markdown frontmatter 或 `<file>.metadata.json` sidecar，未读取 `<directory>/metadata.json`。
- 2026-05-28: 保守评估决策：Round 2 唯一 patch finding 成立，且直接对应 Story 3.4 AC5 / Artifact Path Contract Notes；不降级为 CR TODO，不标记误报。需修复项 1 个，TODO/记录项 0 个，可进入 `bmenhance-cr-03-fixer`。
- 2026-05-28: CR fixer round 2 启动。最新 evaluation 文件为 `3-4-code-review-evaluation-20260528-round-2.md`；仅修复其中 1 个“需要修复”项，不处理非评估确认问题，不修改 Story 文档。
- 2026-05-28: 修复 #1 决策：将 `validateProject` artifact discovery 的目录模式从“只递归普通 files”调整为“发现含 `metadata.json` 的目录即作为 directory artifact entity”，读取 `<directory>/metadata.json` 后以 `metadataLocation: "directory"` 传给 artifact-path rule；未含 metadata 的目录仍继续递归普通 artifact files。
- 2026-05-28: 修复 #1 regression：在 `test/validate-command.test.ts` 的 production validate metadata 测试中新增 `missing-directory/metadata.json` 与 `invalid-directory/metadata.json`，断言分别报告 `artifact-path.missing-required-metadata` 和 `artifact-path.invalid-required-metadata`，并保持输出不泄漏临时绝对路径或原始非法时间值。
- 2026-05-28: CR fixer round 2 验证结果：`npm test -- --run test/validate-command.test.ts` 通过 1/1 文件 11/11 测试；`npm run build` 通过；`npm test` 通过 24/24 文件 144/144 测试；`git diff --check` 通过。
- 2026-05-28: CR round 3 复审启动。Story 文件定位为 `_bmad-output/implementation-artifacts/stories/3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation.md`；CR 目录为 `_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/`；已有 2 个 review summary，本轮为 Round 3。
- 2026-05-28: Round 3 复审上下文：最新 evaluation 修复执行记录标记 directory artifact `<directory>/metadata.json` production validation 已修复；本轮重点验证该闭环，并回归 Round 1 的 legacy config reference、runtime symlink realpath boundary 和 `src/manifest/hash.ts` include traversal 修复。
- 2026-05-28: 当前环境未提供 Agent 子代理工具，按 `bmenhance-cr-01-reviewer` 降级规则改为串行执行 Blind Hunter、Edge Case Hunter、Acceptance Auditor。
- 2026-05-28: CR round 3 验证命令：focused `npm test -- --run test/runtime-path-validation.test.ts test/legacy-namespace-validation.test.ts test/menu-target-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 7/7 文件 40/40 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 144/144 测试；`git diff --check` 通过；仓库没有 `lint` script。
- 2026-05-28: CR round 3 结论：Round 2 directory artifact `<directory>/metadata.json` production validation 已闭环；Round 1 legacy config reference、runtime symlink realpath boundary 和 hash include traversal 修复仍闭环。新增 finding：`src/validation/rules/artifact-path.ts` 对任何 symlink segment 立即报告 `artifact-path.symlink-escape`，定向复现中项目内 symlink 也被误报。结果文件：`_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/3-4-code-review-summary-20260528-round-3.md`。
- 2026-05-28: CR evaluation round 3 启动。按 `bmenhance-cr-02-evaluator` 配置确认 review source 为 `3-4-code-review-summary-20260528-round-3.md`，已有 evaluation round 1-2，本轮 evaluation 编号为 3；未读取或评估其他 Story 的 CR 结果。
- 2026-05-28: 评估证据只读核对：Story issue mapping 将 `artifact-path.symlink-escape` 定义为 artifact path 通过 symlink 逃出 project boundary；`src/validation/rules/artifact-path.ts` 的 `findSymlinkSegment()` 只要 `lstat()` 发现 symlink segment 就立即返回 `artifact-path.symlink-escape`，未做 `realpath()` boundary 判断；`src/validation/rules/runtime-path.ts` 已按 realpath boundary 判断并有 project-internal symlink regression。
- 2026-05-28: 独立复现：临时目录中创建 `_speclite-output/link -> _speclite-output/real`，调用 `validateArtifactPathContract()` 校验 `_speclite-output/link/report.md`，实际返回 `artifact-path.symlink-escape` / `artifact:actualArtifactPath` / `existing-path-segment-is-symlink`。该 symlink target 仍在项目内，确认 Round 3 finding 成立。
- 2026-05-28: 保守评估决策：Round 3 唯一 patch finding 成立，且直接对应 Story 3.4 reserved issue mapping 准确性；不降级为 CR TODO，不标记误报。需修复项 1 个，TODO/记录项 0 个，可进入 `bmenhance-cr-03-fixer`。
- 2026-05-28: CR fixer round 3 启动。最新 evaluation 文件为 `3-4-code-review-evaluation-20260528-round-3.md`；仅修复其中 1 个“需要修复”项，不处理非评估确认问题，不修改 Story 文档。
- 2026-05-28: 修复 #1 决策：`artifact-path` 遇到 symlink segment 时使用 `realpath` 与 project root realpath 比较，只有 symlink target 逃出 target project boundary 才报告 `artifact-path.symlink-escape`；项目内 artifact symlink 不再误报。
- 2026-05-28: 修复 #1 regression：在 `test/artifact-path-validation.test.ts` 新增 project-internal artifact symlink case，断言 `_speclite-output/link -> _speclite-output/real` 且 `actualArtifactPath` 为 `_speclite-output/link/report.md` 时返回 `[]`；同时保留 outside symlink escape regression 并断言 reason 为 `symlink-escape`。
- 2026-05-28: CR fixer round 3 focused 验证结果：`npm test -- --run test/artifact-path-validation.test.ts` 通过 1/1 文件 8/8 测试。
- 2026-05-28: CR fixer round 3 完整验证结果：focused `npm test -- --run test/runtime-path-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts` 通过 3/3 文件 22/22 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 145/145 测试；`git diff --check` 通过。
- 2026-05-28: CR round 4 复审启动。Story 文件定位为 `_bmad-output/implementation-artifacts/stories/3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation.md`；CR 目录为 `_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/`；已有 3 个 review summary，本轮为 Round 4。
- 2026-05-28: Round 4 复审上下文：最新 evaluation 修复执行记录标记 artifact path project-internal symlink 误报已修复；本轮重点验证项目内 symlink 不再误报、symlink escape 仍报告，并回归 Round 1 / Round 2 修复闭环。
- 2026-05-28: 当前环境未提供 Agent 子代理工具，按 `bmenhance-cr-01-reviewer` 降级规则改为串行执行 Blind Hunter、Edge Case Hunter、Acceptance Auditor。
- 2026-05-28: CR round 4 验证命令：focused `npm test -- --run test/runtime-path-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts test/legacy-namespace-validation.test.ts test/menu-target-validation.test.ts test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 7/7 文件 41/41 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 145/145 测试；`git diff --check` 通过；仓库没有 `lint` script。
- 2026-05-28: CR round 4 结论：Round 3 artifact path project-internal symlink 误报修复已闭环，project-external symlink escape 仍报告；Round 1 / Round 2 历史修复未发现回归。结果文件：`_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/3-4-code-review-summary-20260528-round-4.md`。blocking/decision_needed 0，patch 0，defer 0，可进入 evaluator。
- 2026-05-28: CR evaluation round 4 启动。按 `bmenhance-cr-02-evaluator` 配置确认 review source 为 `3-4-code-review-summary-20260528-round-4.md`，已有 evaluation round 1-3，本轮 evaluation 编号为 4；未读取或评估其他 Story 的 CR 结果。
- 2026-05-28: 评估证据只读核对：`src/validation/rules/artifact-path.ts` 已对 symlink segment 使用 `realpath()` 并与 project root realpath 做 native boundary 判断；`test/artifact-path-validation.test.ts` 同时覆盖 project-external symlink escape 和 project-internal symlink accept。`validate-project.ts` directory artifact metadata、`legacy-namespace.ts` installed canonical `SKILL.md` legacy config reference、`runtime-path.ts` symlink realpath boundary、`hash.ts` include traversal 均有源码证据支撑。
- 2026-05-28: CR evaluation round 4 验证命令：focused `npm test -- --run test/runtime-path-validation.test.ts test/artifact-path-validation.test.ts test/validate-command.test.ts test/legacy-namespace-validation.test.ts test/menu-target-validation.test.ts test/ide-target-writer.test.ts test/runtime-structure.test.ts` 通过 7/7 文件 41/41 测试；`npm run build` 通过；完整 `npm test` 通过 24/24 文件 145/145 测试；`git diff --check` 通过；仓库没有 `lint` script。
- 2026-05-28: CR evaluation round 4 结论：Approved / 通过；需修复项 0 个，TODO/记录项 0 个，误报 0 个。满足进入 `bmenhance-cr-04-rules-extractor` / `bmenhance-cr-05-todo-tracker` / `bmenhance-cr-06-finalizer` 的条件。
- 2026-05-28: CR 04 rules extractor 启动。分析范围限定为 `_bmad-output/implementation-artifacts/code-reviews/3-4-code-review/` 的 Round 1-4 review/evaluation/fix records；全局文档只读，不修改 `project-context.md`、architecture 或 specs。
- 2026-05-28: CR 04 量化判定：候选 1「Production artifact validation 必须消费 on-disk metadata entity」硬性门槛通过，评分 8/12，建议去向 rules-summary；候选 2「Symlink escape issue 必须基于 realpath boundary 而非 symlink 存在性」硬性门槛通过，评分 8/12，建议去向 rules-summary。两项均偏 validate / filesystem 技术域，按用户默认授权执行 record-only，不升格全局文档。
- 2026-05-28: CR 04 结果：`cr-rules-summary.md` 追加 Story 3-4 记录，新增 `CR-API-18` 与 `CR-SEC-08` 索引和正文；不修改全局文档。Story 特定闭环项与无复用价值项不另行沉淀。
- 2026-05-28: CR 05 TODO tracker 启动并完成。Round 1-3 findings 均为 evaluator 确认的 P1 阻塞修复项，Round 4 reviewer/evaluator 明确 CR TODO 0；决策：无新增 TODO，`cr-todo-backlog.md` 不修改，不把 blocking 问题降级为 TODO。
- 2026-05-28: CR 06 finalizer 前置验证：最新 evaluation 文件为 `3-4-code-review-evaluation-20260528-round-4.md`，结论为 Approved / 通过；Story 当前状态为 `review`，可更新为 `done`。
- 2026-05-28: CR 06 finalizer 状态同步：Story 文件 `Status: review` 更新为 `Status: done`；`sprint-status.yaml` 的 `3-4-runtime-path-menu-target-legacy-entry-and-artifact-path-validation` 从 `review` 更新为 `done`，`last_updated` 刷新为 `2026-05-28 22:13 CST`。
- 2026-05-28: CR 06 finalizer 容错与 Epic 决策：`_bmad-output/planning-artifacts/bmm-workflow-status.yaml` 不存在，按 skill 规则跳过。Epic 3 下 `3-5`、`3-6` 仍为 `ready-for-dev`，Epic 主状态保持 `in-progress`；无需用户确认修改 Epic 主状态。
