# EXPERIMENT_NOTES

## 实时笔记

- 2026-05-28: 前置核对显示 Epic 3 的 `3-5-commandresult-and-validationissue-json-contract` 当前为 `ready-for-dev`。工作区已有大量既有未提交改动，执行时必须只处理当前 Story 所需范围。
- 2026-05-28: 本轮用户要求只执行 Story 3.5 的 dev-story 开发步骤，因此 3.5 进度文件中的旧 CR/finalizer 计划已收敛为 dev-only；后续不提交、不推送、不触碰无关 Story。
- 2026-05-28: HALT blocker: Story Task 1 明确要求如果 `src/commands/update.ts` 尚不存在，应先完成前置 stories，不得在 Story 3.5 中创建孤立 diagnostics-only scaffold。当前 `find src/commands -maxdepth 1 -type f` 仅发现 `install.ts`、`resolve.ts`、`status.ts`、`validate.ts`；`src/bin/speclite.ts` 也只注册了 `status`、`validate`、`install` 和 `resolve`，无法满足 AC 1/3 对 `speclite update --json` 和 `speclite update --repair --json` 的 covered command 要求。
- 2026-05-28: 主流程复核后维持 HALT。推荐决策是不在 Story 3.5 中补 `update.ts` 或伪造 `update/update.repair` CommandResult，因为这会越过 Story 3.5 的 Scope Boundary 和 Task 1 hard stop。需要先有前置 Story 或单独授权实现 update command scaffold 后，才能恢复 3.5。
- 2026-05-28: Goal 续跑复核后 blocker 仍未解除。当前权威状态：3.1、3.2、3.3、3.4 为 `done`；3.5 为 `in-progress`；3.6 为 `ready-for-dev`。因为 3.5 仍未完成，不能按目标顺序进入 3.6；因为 `update.ts` 前置缺失仍成立，不能启动 CR/evaluator/fixer/finalizer。
- 2026-05-29: 用户确认“按照你的推荐”。当前决策：先把 `src/commands/update.ts` 和 CLI `update` / `update --repair` 注册作为 Story 3.5 的显式前置任务处理；这不是 Story 3.5 的 diagnostics-only scaffold，而是解除 hard stop 的 prerequisite。边界仍然是非写入、非 repair apply、非 Epic 4 业务实现。
- 2026-05-29: 前置 scaffold 已完成并复核。保留的语义是“command surface exists, implementationAvailable false”，用于解除 Story 3.5 Task 1 对 `src/commands/update.ts` 的 hard stop；真实 update plan、conflict detector、repair apply、operation lock 仍归 Epic 4。定向验证和 build 已通过，下一步恢复 `/bmad-dev-story story 3-5`。
- 2026-05-29: Story 3.5 dev-story 已完成。Story 状态为 `review`，`sprint-status.yaml` 对应条目也为 `review`。验证记录显示 focused tests、全量 `npm test`、`npm run build` 和 `git diff --check` 均通过。下一步按原目标启动 fresh `/bmenhance-cr-01-reviewer 3-5`。
- 2026-05-29: Fresh sub-agent 恢复 Story 3.5 后重新执行 resolver/config/context 加载；`python3` 失败，`python3.12` 成功。已完整读取 Story、sprint status、project-context、diagnostics/schema/result/output、issue model、validation aggregation、commands、manifest/source/install-plan/path/adapter/fixture anchors。
- 2026-05-29: 本轮红灯测试集中在 `test/contract-anchors.test.ts`、`test/update-command.test.ts`、`test/validate-command.test.ts`：旧 update placeholder data 被拒绝、repair payload schema 缺失、redaction guard 缺失、config project name 未使用、update conflict single blocker helper 缺失。红灯确认后再实现。
- 2026-05-29: 绿色实现不改 Epic 4 行为。`update` / `update.repair` 输出契约化 empty `updatePlan` / `repairPlan`，`requiresConfirmation: true`、`writeAuthorized: false`、`changedPaths: []`、`skippedPaths: []`、`conflicts: []`；真实 conflict detector 仍未实现。
- 2026-05-29: `ValidationIssueSchema` 增加 issueId category prefix 和 redaction guard；`ide-mirror` / `file-integrity` 的 `suggestedNextStep` 不再拼接 affected path。`source-integrity.unsupported-source` 复用既有 reserved id 表示 invalid module selection，避免新增 taxonomy id。
- 2026-05-29: 最终验证通过：focused Story tests、相关 CLI/diagnostics tests、全量 `npm test`、`npm run build`。Story 与 sprint status 已推进到 `review`；未执行 CR、finalizer、commit 或 push。
- 2026-05-29: Fresh CR reviewer 已执行。Agent 工具不可用，已按 `bmenhance-cr-01-reviewer` 降级为串行三层审查；未执行 evaluator/fixer/finalizer，未修改源码，未提交或推送。
- 2026-05-29: Reviewer 发现 1 个阻塞项：`ValidationIssue.details` 在 Story 和 schema 中都是 optional，但 `findUnsafeIssueValue(undefined)` 返回 unsafe，导致无 `details` 的合法 issue 被 `ValidationIssueSchema` 拒绝。定向复现已记录在 `3-5-code-review-summary-20260529-round-1.md`。
- 2026-05-29: Fresh CR evaluator 已执行。确认 reviewer 发现有效且非误报；无 `details` issue 的定向 `ValidationIssueSchema.safeParse` 结果为 `success: false`，错误路径为 `details`。本轮评估结论为不通过，要求 fixer 只修复 optional `details` guard 与 focused 测试，不扩大到 Epic 4 update/repair 行为。
- 2026-05-29: Fresh CR reviewer Round 2 已执行。Round 1 P1 原始症状已修复：无 `details` 的合法 `ValidationIssue` 现在通过 schema。但复审发现相邻回归：`details: { reason: undefined }` 也会通过 `ValidationIssueSchema`，随后 JSON 渲染为 `{}`，不满足 AC7 的 stable JSON-serializable / fixture-comparable 要求。本轮 reviewer 结论为不通过，结果写入 `3-5-code-review-summary-20260529-round-2.md`。
- 2026-05-29: Fresh CR evaluator Round 2 已执行。独立复现结果：无 `details` issue 通过；`details: { reason: undefined }` 错误通过且 JSON 为 `{}`；`details: { reasons: ["ok", undefined] }` 错误通过且 JSON 为 `{"reasons":["ok",null]}`。评估结论为不通过，finding 有效、非误报、P1，要求 fixer 最小修复 root optional 缺省与 nested non-JSON value 的边界。
- 2026-05-29: Fresh CR reviewer Round 3 已执行。两轮 P1 均已修复：无 `details` issue 通过，object nested `undefined`、array `undefined` 和 unsafe path details 均失败。全量 AC/Tasks 复审发现新阻塞项：`install` command 未复用 `resolveTargetProjectDisplayName`，存在 `_speclite/config.toml` 且 `project_name = " 项目 Install "` 时仍输出临时目录 basename 作为 `targetProject`。本轮 reviewer 结论为不通过，结果写入 `3-5-code-review-summary-20260529-round-3.md`。
- 2026-05-29: Fresh CR evaluator Round 3 已执行。独立核对 AC6 与当前实现后确认 finding 有效：`resolveTargetProjectDisplayName` 会读取 `_speclite/config.toml` 并 trim `core.project_name`，`status` / `validate` / `update` 已使用该 helper，但 `install` 仍使用 `runtime.targetProject` 或 `normalizedTarget.targetProject`，后者来自 target directory basename。临时目录复现结果为 `install = tmp...`，`status/validate/update = 项目 Install`。评估结论为不通过，P1，要求 fixer 最小修复 `install` targetProject display id 并补 focused test。
- 2026-05-29: Fresh CR reviewer Round 4 已执行。三轮 P1 均已修复且无回归：无 `details` issue 通过，object nested `undefined` 与 array `undefined` 均失败，`install` success 与 runtime guard failure 路径均使用 `_speclite/config.toml` 中 trim 后的 `core.project_name`。focused tests、全量 `npm test`、`npm run build` 和定向 `git diff --check` 均通过。本轮 reviewer 结论为通过，结果写入 `3-5-code-review-summary-20260529-round-4.md`。
- 2026-05-29: Fresh CR evaluator Round 4 已执行。独立评估确认 reviewer 通过结论成立：Round 1 optional `details`、Round 2 nested `undefined`、Round 3 install `targetProject` config-name 三个 P1 均已关闭，无剩余必须修复项，不需要 fixer。验证通过：focused tests 6 files / 44 tests，全量 `npm test` 25 files / 154 tests，`npm run build`，定向 `git diff --check`，以及额外复现脚本。
- 2026-05-29: Fresh CR rules extractor 已执行。候选规则判定结果：`ValidationIssue.details` 必须区分 root optional 缺省与 nested non-JSON value、public details 必须 JSON-serializable/redaction-safe、`CommandResult.targetProject` 必须优先使用 trim 后 config project name。这些约束已在 `CONTEXT.md` 和既有 CommandResult / ValidationIssue 规则中覆盖，因此本轮不新增全局规则、不写 `cr-rules-summary.md`，且无需要交给 TODO Tracker 的非阻塞项。
- 2026-05-29: Fresh CR TODO tracker 已执行。已检查 4 轮 CR 记录中的 TODO/非阻塞章节；Round 1-3 都明确“建议纳入 CR TODO：无”，Round 4 evaluator 也确认无需要纳入 CR TODO 的非阻塞项。`cr-todo-backlog.md` 保持不变，当前没有 Story 3.5 新增 TODO。
- 2026-05-29: Fresh CR finalizer 已执行。最新 evaluation 文件为 `3-5-code-review-evaluation-20260529-round-4.md`，结论通过；Story 3.5 与 `sprint-status.yaml` 已同步为 `done`。`bmm-workflow-status.yaml` 不存在已跳过；Epic 3 尚有 Story 3.6 为 `ready-for-dev`，所以 `epic-3` 保持 `in-progress`。
