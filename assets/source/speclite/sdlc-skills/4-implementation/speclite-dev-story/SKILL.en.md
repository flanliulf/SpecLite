---
name: speclite-dev-story
description: "Executes implementation from a context-filled Story `.md`. Use when the user asks to dev story, implement story, implement the next story, continue story development, or provides a story file path. Capable of target Story discovery, red-green-refactor implementation, multi-level testing, DoD validation, and sprint-status synchronization."
allowed-tools: Read, Write, Edit, Grep, Glob, Bash
metadata:
    version: "1.0.3"
    author: "fancyliu"
    catalog: "speclite"
---

[Overview]
    Story implementation engine for the SpecLite implementation phase. It consumes a context-filled Story, implements tasks in order, validates Flow Gates and Definition of Done, then moves the Story to `review`.

    Developer role constraints:
    - Communicate in `{communication_language}` and generate documents in `{document_output_language}`.
    - Only update allowed Story sections: task checkboxes, Dev Agent Record, File List, Change Log, and Status.
    - Follow `references/activation.md` and `references/workflow-steps.md` exactly.

[Core Capabilities]
    - **Activation**: resolve three-tier customization, persistent facts, runtime config, and `workflow.on_complete`; see `references/activation.md`.
    - **Story auto-discovery and review-continuation detection**: support explicit `{story_path}`; otherwise use `sprint-status.yaml` `story_location` or `{implementation_artifacts}/stories` to find the first `ready-for-dev` Story; detect "Senior Developer Review (AI)" and "Review Follow-ups (AI)" sections and extract verdict, unchecked items, and severity counts
    - **Test-driven implementation and quality gates**: enforce red-green-refactor, focused tests, regression checks, lint/static checks, and explicit AC validation.
    - **HALT, Flow Gate, and DoD validation**: run `story-kickoff` gate before state advancement and `story-completion` gate before `review`; HALT on out-of-scope dependencies, 3 consecutive failures, missing required config, or gate failure; NEVER mark a task `[x]` unless tests truly exist and pass
    - **Sprint-status synchronization**: flip `ready-for-dev -> in-progress` and `in-progress -> review` while preserving sprint-status comments and structure.
    - **on_complete terminal directive**: resolve and execute `workflow.on_complete` before exit.

[Conventions]
    - Bare paths resolve from the skill root; `{project-root}` paths resolve from the target project.
    - `{speclite-runtime-root}` is `{project-root}/_speclite`.
    - `{skill-name}` is `speclite-dev-story`.

[Workflow]
    Activation must run before the main workflow. Full rules live in `references/activation.md`; full steps and HALT conditions live in `references/workflow-steps.md`.

    1. Run the activation flow from `references/activation.md`.
    2. Locate and fully load the Story. Prefer explicit `story_path`; otherwise use `sprint-status.yaml` `story_location`, falling back to `{implementation_artifacts}/stories`.
    3. Load project context, Dev Notes, and review-continuation sections.
    4. Run `speclite-flow-gate mode=story-kickoff`; only `PASS` or `PASS_EQUIVALENT` permits `ready-for-dev -> in-progress`.
    5. Implement each task with red-green-refactor, tests, regressions, lint/static checks, and AC validation.
    6. Update File List, Completion Notes, Change Log, and allowed checkboxes only after passing evidence exists.
    7. Fill `Anchor Evidence Summary`, run `speclite-flow-gate mode=story-completion`, then move sprint status to `review` only after DoD and gate success.
    8. Resolve and execute `workflow.on_complete` as the final terminal directive.

[Notes]
    - Read `sprint-status.yaml` end-to-end and preserve comments and STATUS DEFINITIONS.
    - Never implement outside the Story tasks/subtasks.
    - Never mark `[x]` unless tests truly exist and pass.
    - File List must include all changed files using repo-relative paths.
    - DoD and Flow Gate failures HALT immediately.
    - `config.toml.example` is only a field-structure reference, never a runtime fallback.

[Generation Metadata]
    This Skill was generated by skills-creator. Update SKILL.md and SKILL.en.md together, and sync installed copies or manage versions through skills-upgrade.
