---
name: speclite-implementation-readiness-check
description: "Runs the SpecLite Check Implementation Readiness workflow to verify that PRD, UX, Architecture, and Epics are ready for implementation. Use when the user asks to check implementation readiness, run a readiness gate, perform IR, or assess implementation readiness. Capable of config-driven activation, planning artifact reading, step-file auditing, and readiness report output."
allowed-tools: Read, Write, Bash, Grep, Glob, WebSearch
metadata:
  version: "1.0.0"
  author: "fancyliu"
  catalog: "speclite"
---

[Skill Overview]
    Check Implementation Readiness is a Speclite 方案与架构准备 workflow Skill. It executes the migrated workflow in a target project using the configured communication language, document output language, and runtime paths.

    Source entry summary: Validate PRD, UX, Architecture and Epics specs are complete. Use when the user says "check implementation readiness".

[Core Capabilities]
    - **Single UX path discovery**: use Planning root resolver evidence to prefer `{planning_artifacts}/ux/ux-design-specification.md`, read the exact legacy fallback only when canonical is absent, and record `resolvedRoot`, `resolutionMode`, and `actualConsumedPath`.
    - **Readiness output and historical discovery**: write new reports to `{solutioning_artifacts}/implementation-readiness-report/grill-consistency/implementation-readiness-report-{yyyy-MM-dd}.md`; discover existing Planning-root readiness reports and `{planning_artifacts}/ir-grill/` only as in-place historical evidence, without migration, rename, or deletion.
    - **Single whole/sharded discovery contract**: consume PRD, Epics, and Architecture only from `speclite resolve artifact-documents` `consumedPaths`; blocking keeps zero artifact write and zero progress mutation, and selection is invocation-scoped.
    - **Speclite activation**: Resolve three-tier customize, workflow.persistent_facts, workflow.on_complete, and runtime config from merged output of `speclite resolve config --project-root {project-root}`.
    - **Artifact discovery**: Load project artifacts, config fields, historical context, and data files according to the workflow specification.
    - **Step orchestration**: Follow `references/workflow-details.md` and related step files in order, preserving HALT conditions, menus, and state advancement.
    - **Templated output**: Use assets templates or examples to generate documents, reports, specifications, or delivery artifacts in `document_output_language`.
    - **Quality handoff**: Run checklists, status sync, or completion handoff, then resolve and execute `workflow.on_complete` before exit.
    - **Migration alignment**: Depend only on the Speclite runtime model and never on legacy runtime paths, config files, or command namespaces.

[Conventions]
    Bare paths resolve from `{skill-root}`; `{project-root}` is the target project working directory; `{speclite-runtime-root}` is `{project-root}/_speclite`; `{skill-name}` is the skill directory basename.

[Activation]
    Resolve `workflow`, execute prepend steps, load persistent facts, read merged runtime config, communicate in `communication_language`, and execute append steps. Missing config or empty required fields must HALT. `config.toml.example` is only a field-structure reference and must not be used as runtime fallback.

[Workflow]
    1. Fully read `references/workflow-details.md`; it is the authoritative migrated workflow specification. Related references include `references/workflow-details.md`, `references/steps/step-01-document-discovery.md`, `references/steps/step-02-prd-analysis.md`, `references/steps/step-03-epic-coverage-validation.md`, `references/steps/step-04-ux-alignment.md`, `references/steps/step-05-epic-quality-review.md`, and other reference files.
    2. Confirm `{skill-root}`, `{project-root}`, `{speclite-runtime-root}`, and `{skill-name}` before execution.
    3. If the workflow points to step files, read only the current step file, complete it, and then proceed to the next one.
    4. Preserve source workflow requirements for templates, checklists, status fields, and output locations.
    5. Before exit, run the quality checks, resolve `workflow.on_complete`, and append `*This document was generated by the speclite-implementation-readiness-check Skill*` to generated documents.

[Notes]
    - The directory name and YAML `name` must remain `speclite-implementation-readiness-check`.
    - `references/workflow-details.md` and related references are executable instructions, not background reading.
    - `config.toml.example` is only a reference and is never a runtime fallback.
    - The current workflow must not depend on legacy runtime directories, YAML config, or command namespaces.

[Generated]
    This Skill was generated by speclite-skill-creator.
