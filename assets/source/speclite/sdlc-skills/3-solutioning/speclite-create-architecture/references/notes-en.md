# Operational Notes (EN)

Read these notes alongside `SKILL.en.md`. They are mandatory rules for the skill's operation.

## Naming & Languages

- Kebab-case-consistent name/dir/YAML: `speclite-create-architecture`
- Communicate in `{communication_language}`; produce documents in `{document_output_language}`

## Activation Hard Rules

- The 6 activation steps **must** precede workflow execution
- Missing `customize.toml` files may be skipped, but the merge rules cannot be omitted

## Collaboration & Step Reading

- 🛑 **Never** generate content without user input
- 📖 **Critical**: each step file must be **read in full** before any action; partial understanding leads to incomplete decisions
- 🔄 **Critical**: when loading the next step via `C`, ensure the entire step file has been read and understood before advancing
- ✅ Always treat this as collaborative discovery between architecture peers
- 📋 You are the facilitator, **not** a content generator

## Forbidden Practices

- ⚠️ **Absolutely forbid time estimation** — AI development speed has fundamentally changed
- 🌐 All technology versions must be verified live via WebSearch; **forbid** hard-coded version numbers
- The project tree must be complete, concrete, and executable; **forbid** generic placeholders

## Workflow Closure Rules

- The A/P/C menu is a mandatory closure for every content-generating step: only `C` persists and advances; A/P must return to the A/P/C menu and wait for the final selection
- The continuation branch (Step 1b) must preserve existing content; `X` reset must require y/n second confirmation
- Missing PRD is a HALT condition — never bypass
- Completion must execute `workflow.on_complete` resolution and run the returned value as the final terminal directive
- The Architecture document footer must append `*Generated by speclite-create-architecture Skill*`
