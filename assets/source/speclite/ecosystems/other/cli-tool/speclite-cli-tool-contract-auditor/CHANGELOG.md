# Changelog

本文件记录 `speclite-cli-tool-contract-auditor` 技能的版本变更历史。

## [1.0.0] - 2026-07-06

### Added

- 新增 CLI tool other ecosystem seed Skill。
- 覆盖 `bin` entry、command surface、TTY / non-TTY output、exit code、JSON contract、shell portability 和 install smoke evidence。
- 明确该 Skill 是 CLI tool project-shape companion，不替代通用 CLI output、Dev Story 或 Code Review workflow。
