# Changelog

本文件记录 `speclite-npm-package-project-auditor` 技能的版本变更历史。

## [1.0.0] - 2026-07-06

### Added

- 新增 npm package other ecosystem seed Skill。
- 覆盖 `package.json`、package manager、publish metadata、tarball / `npx` / package surface、release gate 和 library / CLI smoke evidence。
- 明确该 Skill 是 project-shape-specific audit companion，不迁移、不替代 `sdlc-skills/5-devops/speclite-npm-publisher`。
