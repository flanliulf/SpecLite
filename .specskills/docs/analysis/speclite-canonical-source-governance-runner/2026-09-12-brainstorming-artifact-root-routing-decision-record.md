# Canonical Source Governance Decision Record

| Field | Value |
|---|---|
| Date | 2026-09-12 |
| Change Scope | Brainstorming artifact root 收口到 `SPEC 09` 的 `{brainstorming_artifacts}`：`core-skills/module-help.csv` 的 `output-location` 由 `{output_folder}/brainstorming` 改为 `{brainstorming_artifacts}`；`speclite-brainstorming` canonical package 的 `references/workflow.md` 增加 `speclite resolve artifact-roots` 解析步骤并把 `brainstorming_session_output_file` 改写为 `{brainstorming_artifacts}/...`、`references/steps/step-01-session-setup.md` 的既有 session 扫描目录同步、`config.toml.example` 补 `core.brainstorming_artifacts`、`CHANGELOG.md` 记 `1.1.0`。伴随本轮 pristine-install 修复（`file-integrity.unknown-ownership` 收紧、repair protected skip、`artifact-path.missing-required-artifact` 的 `not-yet-produced` 分档），重生成 fresh-install fixture installed-state 基线与 packaging manifest。 |
| Governance Classes | `canonical-source-truth` (`D0`)、`module-discovery-contract` (`D0`)、`release-evidence` (`D0`)、`current-public-docs` (`D1`)、`living-legacy-reference` (`D2`)、`frozen-historical-record` (`D2`) |
| D0 Findings | `speclite-check-canonical-source-change` warn 与 `--mode strict` 均为 `status: ok`、`findings: []`、0 error。Counts 无回退：`core=19`、`sdlc=50`、`support=8`、`hooks=2`、`ecosystems.totalPackageRoots=8`、`defaultInstall.total=69`。`triggeredRules: ["core-or-sdlc-package-change"]`，`impactedClasses` 为 `canonical-source-truth` 与 `module-discovery-contract`。 |
| D1 Decisions | `docs/reference/workflow-artifact-layout.md`、`docs/explanation/file-ownership-model.md`、`docs/how-to/update-and-repair.md`、`docs/how-to/process-governance-report.md`、`docs/reference/command-result-json.md` 已更新；`assets/source/speclite/README.md` / `README.en.md` 与 `docs/reference/canonical-source-layout.md` 经核对无需更新。详见下表。 |
| D2 Decisions | `docs/legacy/BMAD_SPECLITE_SKILL_MAPPING.md` skipped；`docs/legacy/HANDOFF.md`、`skill-description-normalization/{PLAN,EXPERIMENTS,EXPERIMENT_NOTES}.md`、`docs/legacy/CANONICAL_SKILL_ITERATION_CONTEXT.md` 未被 broad grep 命中，historical snapshot 保持不变。详见下表。 |
| Verification | `npm test -- test/canonical-source-change-check-script.test.ts test/canonical-source-change-check-hook.test.ts` 2 files / 6 passed；`npx vitest run` 68 files / 731 passed / 4 todo / 0 failed；`npm run build` 成功；`npm run docs:check` 通过（72 Markdown、5 drafts）；`npm run release:packaging-check` 通过且 `release/` 与 `dist/packaging-manifest.json` 的 `packageHash` 一致；`check_skill_density.py assets/source/speclite/core-skills/speclite-brainstorming` exit 0（`SKILL.md` body 1140 chars，`has_workflow_reference: true`，无 density warning）；`git diff --check` exit 0。 |

## Decisions

| Surface | Decision | Reason | Evidence |
|---|---|---|---|
| `assets/source/speclite/core-skills/module-help.csv` | updated | `module-discovery-contract` (`D0`)。`speclite-brainstorming` 行的 `output-location` 仍是 Epic 11 之前的 `{output_folder}/brainstorming`，导致 manifest generator 把 phase-coverage `artifactContract.defaultOutputPath` 解析成安装器根本不创建的 `_speclite-output/brainstorming`，fresh install 后 `validate` 立即产生指向该路径的 artifact-path issue。按 `SPEC 09` 的 `core.brainstorming_artifacts` / `{brainstorming_artifacts}` 收口。 | `git diff` 第 4 行 `{output_folder}/brainstorming` → `{brainstorming_artifacts}`（1 insertion / 1 deletion）。fixture 基线 `phase-coverage-full.json` 中 `artifactType: "brainstorming-session"` 的 `defaultOutputPath` 由 `_speclite-output/brainstorming` 变为 `_speclite-output/0-brainstorming-artifacts`。strict checker `status: ok`，`core=19` 未变。 |
| `assets/source/speclite/core-skills/speclite-brainstorming/references/workflow.md` | updated | `canonical-source-truth` (`D0`)。Workflow 原先直接拼 `{output_folder}/brainstorming/...`，属于 `SPEC 09` 明令禁止的「从 Skill package 文案手写 fallback」。改为先 `speclite resolve artifact-roots --project-root {project-root}`，消费 `brainstorming_artifacts.resolvedRoot`、保留 `resolutionMode` 与 provenance，缺失即 HALT；写法与已收口的 `speclite-product-brief/references/workflow-details.md` 对齐。 | `git diff` +9/-1：新增 artifact-roots 解析段；`brainstorming_session_output_file` 改为 `{brainstorming_artifacts}/brainstorming-session-{{date}}-{{time}}.md`。 |
| `assets/source/speclite/core-skills/speclite-brainstorming/references/steps/step-01-session-setup.md` | updated | `canonical-source-truth` (`D0`)。既有 session 扫描目录必须与输出目录同源，否则续写会去旧路径找历史 session。 | `git diff` 第 36 行 `{output_folder}/brainstorming/` → `{brainstorming_artifacts}/`（1 insertion / 1 deletion）。 |
| `assets/source/speclite/core-skills/speclite-brainstorming/config.toml.example` | updated | `canonical-source-truth` (`D0`)。该文件是「expected fields 结构参考」，workflow 现在消费 `core.brainstorming_artifacts`，字段必须出现在参考里；与 `speclite-correct-course/config.toml.example` 等已收口 skill 的写法一致。 | `git diff` `[core]` 段新增 `brainstorming_artifacts = "{project-root}/_speclite-output/0-brainstorming-artifacts"`（1 insertion）。 |
| `assets/source/speclite/core-skills/speclite-brainstorming/CHANGELOG.md` | updated | `canonical-source-truth` (`D0`)。Canonical Skill Package 的契约性变更必须记入随包 CHANGELOG，并显式声明 existing-install 兼容性边界。 | `git diff` 新增 `## [1.1.0] - 2026-09-12`（13 insertions），含「变更」四条与「兼容性」一条：existing install 缺 `core.brainstorming_artifacts` 时由 resolver 按 `SPEC 09` fallback 到 `{output_folder}/brainstorming`，本 Skill 不迁移既有 brainstorming artifacts。 |
| `assets/source/speclite/core-skills/speclite-brainstorming/`（package 整体） | updated | `canonical-source-truth` (`D0`)。Package 内容变更后 `canonicalPackageHash` 必须随之刷新，否则 IDE mirror 校验与 repair baseline 失配。 | `check_skill_density.py` exit 0、无 warning；fixture 基线 `skill-index-full.json` 中 `speclite-brainstorming` 的 `canonicalPackageHash` 由 `sha256:2bb53026...` 刷新为 `sha256:43ae5979...`，由真实 fresh install 重新导出而非手工编辑。 |
| `release/packaging-manifest.json` / `dist/packaging-manifest.json` | updated | `release-evidence` (`D0`)。Packaged canonical source 内容变化后必须重生成 release evidence，否则发布包 hash 与 canonical source 不一致。 | `npm run release:packaging-check` 通过；`packageHash` 由 `sha256:75675032...` 刷新为 `sha256:4b3ffa2e...`（仅此一行变化，文件清单不变）；manifest 含 `module-help.csv` 与 `speclite-brainstorming/` 共 15 个 canonical 条目；`release/` 与 `dist/` 两份 manifest 的 `packageHash` 一致。 |
| `test/fixtures/fresh-install-empty-project/expected/installed-state/{files-index-full,skill-index-full,phase-coverage-full}.json` | updated（historical snapshot 刷新） | `canonical-source-truth` (`D0`) 的派生确定性基线。Canonical package 内容与 module-help 路由变化后必须刷新，否则 release gate fixture 失配。基线由真实 `install` 再生，未手工编辑任何 hash。 | `test/fixture-release-gates.test.ts` 10/10 通过（含本轮新增两条 pristine install gate）；diff 仅含 brainstorming package hash（`skill-index-full`、`files-index-full` 共 20 行）与 `defaultOutputPath`（`phase-coverage-full` 1 行）。 |
| `docs/reference/workflow-artifact-layout.md` | updated | `current-public-docs` (`D1`)。Root and Planning Artifacts 表仍把 brainstorming 输出写成 `brainstorming/brainstorming-session-{date}-{time}.md`，与新 canonical 路由不一致，读者会看到旧行为。 | `git diff` 第 158 行改为 `{brainstorming_artifacts}/brainstorming-session-{date}-{time}.md`，并注明输出根来自 `speclite resolve artifact-roots`（1 insertion / 1 deletion）。`npm run docs:check` 通过。 |
| `docs/explanation/file-ownership-model.md` | updated | `current-public-docs` (`D1`)。本轮 repair 行为变更（protected ownership 由 conflict 改为 planned skip）与 `file-integrity.unknown-ownership` 收紧必须在 ownership 解释文档中体现；原文 human-owned 清单也漏了 `.gitignore`，而 `src/update/ownership-model.ts` 的 `isHumanOwnedCustomPath` 一直把它分类为 human-owned。 | `git diff` +15/-5：human-owned 清单补 `.gitignore`；新增 fresh install 登记的 stub 属 expected pristine state 的说明；repair 行为改为 `skip` 且不产生 conflict；Lifecycle Matrix 的 repair 列与 `unknown` 行修正为 `conflict`；Validation Relationship 增加 `unknown-ownership` 反向检查段。 |
| `docs/how-to/update-and-repair.md` | updated | `current-public-docs` (`D1`)。用户按旧文档会认为 pristine install 上 repair 被 protected path 阻断是预期行为。 | `git diff` +4：Preview Repair Plan 段说明 protected path 为 `action: "skip"`、授权后出现在 `skippedPaths` 且内容不变；Handle Conflicts 段说明 `conflicts` 只保留 unknown ownership / path escape / missing source evidence / unsupported repair。 |
| `docs/how-to/process-governance-report.md`、`docs/reference/command-result-json.md` | updated | `current-public-docs` (`D1`)。`GovernanceReportData.metrics` 新增 `notYetProducedCount` 且 `openGapCount` 语义收窄，公开 JSON 消费者参考必须同步，否则 automation 会按旧定义解读空项目的 `openGapCount`。 | `git diff` 分别 +5/-2 与 +3/-1：指标数量由四个改为五个，新增 `notYetProducedCount` 行，`openGapCount` 注明不含 `not-yet-produced`。契约依据为 `SPEC 10` 与 `SPEC 07` 本轮修订。 |
| `assets/source/speclite/README.md`、`assets/source/speclite/README.en.md` | skipped | `current-public-docs` (`D1`)。两份 README 只列 `speclite-brainstorming` 的 skill id 与 core-skills 分类说明，不承载 artifact 输出路径；support/core package roots count 未变（`core=19`、`support=8`）。 | `grep -n "brainstorming"` 命中 `README.md:83`、`README.en.md:9,63`，全部为 skill id 或分类描述，无路径事实。strict checker `counts` 无漂移。 |
| `docs/reference/canonical-source-layout.md` | skipped | `current-public-docs` (`D1`)。本轮未新增或删除 package root，snapshot 表中的 core / sdlc / support / ecosystem counts 全部保持不变。 | strict checker `counts`：`core=19`、`sdlc=50`、`support=8`、`hooks=2`、`ecosystems.totalPackageRoots=8`、`defaultInstall.total=69`，与文档现值一致。 |
| `assets/source/speclite/docs/legacy/BMAD_SPECLITE_SKILL_MAPPING.md` | skipped | `living-legacy-reference` (`D2`)。broad grep 命中第 34 行，但该行只记录 `bmad-brainstorming` → `core-skills/speclite-brainstorming` 的 DIRECT 迁移映射，不含 artifact 输出路径事实；映射关系本身未变，改动它不会指导任何后续迁移决策。 | `grep -n "brainstorming" assets/source/speclite/docs/legacy/BMAD_SPECLITE_SKILL_MAPPING.md` 仅 1 处命中，为 skill id 映射行。 |
| `assets/source/speclite/docs/legacy/CANONICAL_SKILL_ITERATION_CONTEXT.md` | skipped | `living-legacy-reference` (`D2`)。broad grep 零命中，本轮无任何需要同步的 living reference 内容。 | `grep -n "brainstorming"` 无输出。 |
| `assets/source/speclite/docs/legacy/HANDOFF.md`、`assets/source/speclite/skill-description-normalization/{PLAN,EXPERIMENTS,EXPERIMENT_NOTES}.md` | historical snapshot | `frozen-historical-record` (`D2`)。四份文件是历史执行快照；broad grep 对 `brainstorming` 与 `output_folder` 均零命中，既无需要修订的前瞻性引用，也不存在会误导后续维护的旧路径。按 updatePolicy 保留过去事实，不改写。 | `grep -n "brainstorming\|output_folder"` 对四份文件均无输出；`find assets/source/speclite -name "PLAN.md" -o -name "EXPERIMENTS.md" -o -name "EXPERIMENT_NOTES.md" -o -name "HANDOFF.md"` 确认 governance 映射内的 frozen 文件只有这四份。 |
| `_bmad-output/**`（readiness reports、PRD/Epics、config-audit results）与 `_bmad/core/module-help.csv` | historical snapshot | 不在 `canonical-governance.json` 的任何 pathGlob 内。`_bmad-output/**` 是 workflow-owned 历史过程产物（readiness report、需求清单、config audit 快照），其中的 `{output_folder}/brainstorming` 是 `NFR14a` 明确定义的 existing-install fallback 语义，仍然成立且必须保留；`_bmad/` 是上游 BMAD vendor source，不属于 SpecLite canonical source。 | `grep -rn "output_folder}/brainstorming"` 命中项全部位于 `_bmad-output/`、`_bmad/` 与 `specs/09-sdlc-workflow-lifecycle-contract.md:66`（后者正是 fallback 契约本身，语义未变）。`git status --short` 确认三者本轮零改动。 |

---

*本文档由 speclite-canonical-source-governance-runner Skill 自动生成*
