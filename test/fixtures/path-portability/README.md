# Path Portability Fixture

This release gate fixture verifies project-relative POSIX paths, no absolute path leaks, LF/source hash preservation, executable intent, shell invocation stability, terminal no-color output, and the explicit `update --repair --json` boundary.

It is not a documentation example and must not be included in the npm package.
