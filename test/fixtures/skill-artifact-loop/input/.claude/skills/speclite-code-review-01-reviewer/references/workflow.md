# Fixture Workflow

This local fixture writer consumes only the installed package, resolved config, and resolved customization.
It does not call an LLM, agent runtime, IDE automation, network service, or source checkout prompt.
